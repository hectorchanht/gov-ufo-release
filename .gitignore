# ============================================================
# OFFLINE MIRROR — large files excluded from Git
# Run ./scripts/sync.sh after clone to populate locally.
# ============================================================

# Bundle archives (big zips)
bundles/Release_1.zip
bundles/uapvideos.zip
bundles/release_02_document_bundle.zip
bundles/uap052226.zip

# DVIDS / DOD bulk videos — ALL excluded from git.
# Site streams them from the videos-v1 / wargov-r02-v1 GitHub Releases instead.
bundles/uapvideos/
bundles/uap052226/

# PDFs all moved to GitHub Releases (pdfs-v1 / wargov-r02-v1) — the repo keeps
# only images for fast page loads. Source: ./scripts/sync.sh re-populates.
bundles/Release_1/*.pdf
bundles/release_02_document_bundle/*.pdf
aaro/pdfs/
nasa/pdfs/
nara/pdfs/
geipan/pdfs/
uk/pdfs/
brazil/pdfs/
chile/pdfs/
nz/pdfs/
canada/pdfs/
argentina/pdfs/
uruguay/pdfs/
peru/pdfs/
spain/pdfs/
italy/pdfs/

# Local extraction cache
download.zip

# AARO official UAP videos (cloudfront, often >100 MB)
aaro/videos/
geipan/videos/

# Build / runtime artifacts
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
node_modules/

# Generated manifest cache (rebuilt by scripts)
.cache/

# Editor cruft
.vscode/
.idea/

# Generated PDF text extracts (re-derived from .pdf by scripts/extract-pdf-text.py)
**/.pdftext/

# SSG migration (Phase 2+)
node_modules/
dist/
.pnpm-store/
.wrangler/
# Astro types cache (regenerated by `astro sync` + `astro build`; tsconfig
# references .astro/types.d.ts but it MUST stay generated, not tracked).
.astro/
tests/.playwright-cache/
# Playwright test runner output — diff PNGs, traces, video on failure.
# Baselines live under tests/visual-baselines/ and ARE tracked.
test-results/
playwright-report/
.lighthouseci/
.env
.dev.vars
*.pre-bounce.md

# Claude Code local agent state / worktrees
.claude/
