---
status: testing
phase: 04.1-legacy-reorg-stories-site-pages-nav-surface
source:
  - 04.1-01-SUMMARY.md
  - 04.1-02-SUMMARY.md
  - 04.1-03-SUMMARY.md
  - 04.1-04-SUMMARY.md
  - 04.1-05-SUMMARY.md
  - 04.1-06-SUMMARY.md
  - 04.1-07-SUMMARY.md
started: 2026-06-02T13:30:00Z
updated: 2026-06-02T13:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Boot preview + War.gov landing
expected: |
  `pnpm build` succeeds. `python3 -m http.server 4321 --directory dist` serves dist/.
  Open http://127.0.0.1:4321/ — War.gov landing loads. Nav shows: WAR.GOV · AARO · NASA · NARA · STORIES ▾ · ABOUT ▾.
  Footer has 6 columns (License, Source, Archives, Pages, Stories, Colophon). No console errors.
awaiting: user response

## Tests

### 1. Boot preview + War.gov landing
expected: |
  `pnpm build` succeeds. Open http://127.0.0.1:4321/ — War.gov landing loads.
  Nav: WAR.GOV · AARO · NASA · NARA · STORIES ▾ · ABOUT ▾. Footer has Pages + Stories columns.
  No console errors.
result: [pending]

### 2. Stories index page
expected: |
  Visit /stories/. Hero "Stories". Grid of 46 case cards. Each card shows title, archive label, date, lede.
  Click any card → navigates to /stories/{slug}/. Pagefind search input accepts a query.
result: [pending]

### 3. Story detail — Tic Tac (active archive: AARO)
expected: |
  Visit /stories/tic-tac/. Hero shows "Tic Tac (2004 USS Nimitz)" + AARO source link.
  Body renders verbatim AARO Tic Tac content. EXACTLY one nav (top) + one footer (bottom).
  No legacy chrome (no duplicate header, no `<style>` block inside article). AARO blue accent (`--caution: #4a9eff`).
result: [pending]

### 4. Story detail — Rendlesham (dormant archive: UK) — B-1 proof
expected: |
  Visit /stories/rendlesham/. Page renders. View page source → find `data-pagefind-body`.
  Pagefind query "rendlesham" returns ≥1 hit linking to /stories/rendlesham/.
  This proves the B-1 fix — dormant-archive stories are NOT excluded from search.
result: [pending]

### 5. Site page — /about/
expected: |
  Visit /about/. Hero "About realufo.org". Body renders legacy about.html content verbatim, wrapped in
  RootLayout (Nav + Footer + scanlines). Nav highlights ABOUT ▾ dropdown trigger.
  No `<style>` / `<link rel=stylesheet>` inside `<article class="page-body">`.
result: [pending]

### 6. Site page interactive — /map/ + /timeline/
expected: |
  Visit /map/. Interactive map renders with case pins (preserved Leaflet JS works).
  Visit /timeline/. Chronological timeline UI works (preserved init script fires).
  No JS console errors on either page. Both pages have nav + footer.
result: [pending]

### 7. Nav dropdown desktop (≥720 px)
expected: |
  At desktop width, hover STORIES ▾ → dropdown reveals 8 featured stories (Tic Tac, Gimbal, Roswell,
  Rendlesham, Trans-en-Provence, Varginha, Falcon Lake, Phoenix Lights) + divider + "All stories →".
  Hover ABOUT ▾ → dropdown reveals 6 site pages (About, FOIA, Glossary, Map, Timeline, What's New).
  Pure CSS reveal — no JS framework. Esc closes panel.
result: [pending]

### 8. Nav dropdown mobile (360 px)
expected: |
  Resize to 360 px. Tap hamburger ☰ → drawer opens.
  STORIES + ABOUT sections collapse with [+]/[−] toggle.
  Tap STORIES section header → 8 stories list expands.
  Tap ABOUT section header → 6 site pages list expands.
  Each dropdown item touch target ≥ 44 px. No horizontal scroll on any page.
result: [pending]

### 9. Footer new columns
expected: |
  Scroll to footer on any page.
  "Pages" column lists 6 site pages with internal links (no ↗ marker).
  "Stories" column lists "All stories →" + 4–6 featured stories.
  Footer grid reflows: 6 columns desktop → 3 columns ~720-1023 px → 1 column <720 px.
  External Source links retain ↗ marker (footer external links allowed per CLAUDE.md §11).
result: [pending]

### 10. Mobile-first 360 px — no horizontal scroll
expected: |
  Open DevTools console at 360 px. Run `document.documentElement.scrollWidth === window.innerWidth`.
  Returns `true` on /, /stories/, /stories/tic-tac/, /about/, /foia/, /map/, /timeline/.
  No element overflows viewport.
result: [pending]

### 11. Pagefind search
expected: |
  On /search.html (or /stories/ search input), query "tic-tac" → ≥1 result.
  Query "FOIA" → ≥1 result (site-page indexed).
  Query "glossary" → ≥1 result.
  Query "trans-en-provence" → ≥1 result (B-1 dormant proof).
  Query "varginha" → ≥1 result.
  Query "falcon-lake" → ≥1 result.
  Results link to correct /stories/{slug}/ or /{site-page}/ canonical.
result: [pending]

### 12. Redirects + URL contract (DEFERRED to CF Pages preview)
expected: |
  This test is INFORMATIONAL ONLY locally — static server bypasses `_redirects`.
  Inspect `_redirects` directly: contains 301 for /aaro/tic-tac.html → /stories/tic-tac/,
  /about.html → /about/, /donate.html → /about/ (or 410).
  Skip unless you have a CF Pages deploy preview URL to test against.
result: [pending]

## Summary

total: 12
passed: 0
issues: 0
pending: 12
skipped: 0
blocked: 0

## Gaps

[none yet]
