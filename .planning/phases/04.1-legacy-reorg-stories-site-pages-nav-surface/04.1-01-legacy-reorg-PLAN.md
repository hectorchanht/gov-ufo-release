---
phase: 04.1-legacy-reorg-stories-site-pages-nav-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - legacy/ (new directory — all moved legacy HTML lands here)
  - aaro/ (every *.html and aaro/assets/, aaro/pages/ moved out)
  - nara/ (every *.html and nara/assets/, nara/pages/ moved out)
  - nasa/ (story.html and nasa/assets/ moved out)
  - geipan/ (entire directory moved)
  - uk/ (entire directory moved)
  - brazil/ (entire directory moved)
  - chile/ (entire directory moved)
  - argentina/ (entire directory moved)
  - canada/ (entire directory moved)
  - italy/ (entire directory moved)
  - nz/ (every *.html and nz/assets/ moved — Astro owns /nz/ via src/pages/nz/index.astro)
  - peru/ (entire directory moved)
  - spain/ (entire directory moved)
  - uruguay/ (every *.html and uruguay/assets/ moved — Astro owns /uruguay/)
  - 404.html
  - about.html
  - compare.html
  - donate.html
  - embed.html
  - foia.html
  - glossary.html
  - index.html
  - map.html
  - stats.html
  - timeline.html
  - whatsnew.html
  - scripts/snapshot-urls.py
  - .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md
autonomous: true
requirements:
  - SSG-10
  - SSG-11
tags:
  - legacy-reorg
  - file-move
  - phase-04.1

must_haves:
  truths:
    - Repository root contains no .html files outside src/, public/, dist/, tests/, .planning/, legacy/, node_modules/.
    - legacy/ directory exists and contains every previously-root legacy .html plus every legacy archive subdirectory.
    - Git history for moved files is preserved via git mv (renames not deletions).
    - Astro-owned routes (src/pages/, public/, assets/, slideshow/, slideshow-2/, bundles/, data/, src/data/, scripts/, workers/, tests/, .planning/, config files, package files) are untouched.
    - scripts/snapshot-urls.py excludes legacy/ from URL enumeration (no /legacy/* routes ever emitted into URL-CONTRACT.txt after Task 4).
    - 04.1-legacy-html-structure-audit.md exists and inventories which legacy files contain <main>, <article>, or neither — driving the extractor cascade in Plans 04.1-03/04.
  artifacts:
    - path: legacy/aaro/tic-tac.html
      provides: AARO Tic-Tac story sub-page after move
      contains: "Nimitz"
    - path: legacy/about.html
      provides: legacy root About page after move
      contains: "realufo"
    - path: legacy/geipan/index.html
      provides: dormant GEIPAN landing after wholesale dormant-archive move
      contains: "GEIPAN"
    - path: legacy/nz/kaikoura.html
      provides: NZ Kaikoura story sub-page after move (NZ index stays Astro-owned, sub-pages move)
      contains: "Kaikoura"
    - path: scripts/snapshot-urls.py
      provides: updated EXCLUDE_DIR_PREFIXES blocking legacy/ from URL enumeration
      contains: "'legacy/'"
    - path: .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md
      provides: per-file inventory of <main>/<article>/<section>/<div> structural anchors in legacy HTML
      contains: "## Anchor inventory"
  key_links:
    - from: working tree root
      to: legacy/
      via: git mv (rename, not delete+add)
      pattern: "^R[0-9]+\\s+.*\\s+legacy/"
    - from: scripts/snapshot-urls.py EXCLUDE_DIR_PREFIXES
      to: URL-CONTRACT.txt enumeration
      via: is_excluded() path filter
      pattern: "'legacy/',"
---

## Goal

Move every legacy root HTML file and every legacy archive subdirectory listed in CONTEXT.md `<decisions>` "Legacy reorg layout" into a new `legacy/` directory at the repo root using `git mv` so history is preserved, leaving Astro-owned routes (`src/pages/`, `public/`, `assets/`, `slideshow/`, `slideshow-2/`, `bundles/`, `data/`, `src/data/`, `scripts/`, `workers/`, `tests/`, `.planning/`, config files) untouched. Then (Task 4) extend `scripts/snapshot-urls.py` to exclude `legacy/` from URL enumeration BEFORE plan 04.1-06 regenerates URL-CONTRACT.txt, and (Task 5) audit which legacy HTML files contain `<main>`, `<article>`, or neither so the extractors in plans 04.1-03/04 have a documented selector cascade to consume.

## Must-haves

(See frontmatter `must_haves`.)

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Create legacy/ and move all root-level legacy .html files</name>
  <files>
    legacy/404.html, legacy/about.html, legacy/compare.html, legacy/donate.html,
    legacy/embed.html, legacy/foia.html, legacy/glossary.html, legacy/index.html,
    legacy/map.html, legacy/stats.html, legacy/timeline.html, legacy/whatsnew.html
  </files>
  <read_first>
    - /Users/laichan/code/war-gov-ufo-release/.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-CONTEXT.md (§ decisions > Legacy reorg layout)
    - /Users/laichan/code/war-gov-ufo-release/CLAUDE.md (§5 storage layout — confirm which dirs are NOT legacy)
    - Output of `ls *.html` at repo root before moving (you should see exactly the 12 files listed in CONTEXT.md `<specifics>` "Root site-pages to move").
  </read_first>
  <action>
Run `mkdir -p legacy` then `git mv` each of the 12 root-level .html files (per CONTEXT.md `<specifics>` "Root site-pages to move"):
404.html, about.html, compare.html, donate.html, embed.html, foia.html, glossary.html, index.html, map.html, stats.html, timeline.html, whatsnew.html.

Use `git mv <file> legacy/<file>` one file per command (NOT a wildcard) so each move shows up as a rename in `git status`. Do NOT touch any other .html file in subdirectories — those are handled in Task 2 and Task 3.

If a file is missing (e.g. someone already moved 404.html), skip it with a warning and continue — do NOT fail the task.

Do NOT touch `src/pages/index.astro` or any file under `src/`, `public/`, `dist/`, `tests/`, `.planning/`, `.lighthouseci/`, `test-results/`, or `node_modules/`. Do NOT move `assets/`, `slideshow/`, `slideshow-2/`, `bundles/`, `data/`, `release-manifest.json`, `uap-release001.csv`, `uap-data.csv`, `_redirects`, `_headers`, `URL-CONTRACT.txt`, or any config file (`astro.config.mjs`, `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `wrangler.toml`, `playwright.config.ts`, `.lighthouserc.cjs`, `pagefind.json`, `.gitignore`, etc.).
  </action>
  <verify>
    <automated>test -d legacy && test -f legacy/about.html && test -f legacy/404.html && test -f legacy/index.html && test -f legacy/glossary.html && [ "$(ls *.html 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && git status --porcelain | grep -E '^R[0-9]*\s+.*\s+->\s+legacy/(about|404|index|glossary|map|timeline|whatsnew|foia|stats|donate|embed|compare)\.html$' | wc -l | awk '{ if ($1+0 >= 10) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `ls *.html` at repo root returns zero files.
    - `legacy/about.html`, `legacy/404.html`, `legacy/index.html`, `legacy/glossary.html` all exist as regular files.
    - `git status` shows rename entries (`R`) for ≥ 10 of the 12 files (allowing for any already-missing ones); each renamed entry points into `legacy/`.
    - No file under `src/`, `public/`, `assets/`, `slideshow/`, `slideshow-2/`, `bundles/`, `data/`, `scripts/` is in the porcelain output.
  </acceptance_criteria>
  <done>Repo root has zero .html files; legacy/ holds the 12 (minus any that were already absent); git renames are visible in `git status`.</done>
</task>

<task type="auto">
  <name>Task 2: Move partial-port archive HTML sub-pages (aaro, nara, nasa, nz, uruguay) into legacy/</name>
  <files>
    legacy/aaro/ (all *.html + aaro/assets/ + aaro/pages/),
    legacy/nara/ (all *.html + nara/assets/ + nara/pages/),
    legacy/nasa/ (story.html + nasa/assets/),
    legacy/nz/ (all *.html + nz/assets/),
    legacy/uruguay/ (all *.html + uruguay/assets/)
  </files>
  <read_first>
    - /Users/laichan/code/war-gov-ufo-release/.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-CONTEXT.md (§ decisions "Legacy reorg layout" — the partial-port archives list, and "NOT moved: Astro-owned routes (src/, public/)")
    - /Users/laichan/code/war-gov-ufo-release/scripts/copy-legacy-archives.sh lines 88-142 (current partial-port logic — confirms that index.html of these archives is owned by Astro and the rest IS legacy)
    - Output of `git ls-files aaro/` `git ls-files nara/` `git ls-files nasa/` `git ls-files nz/` `git ls-files uruguay/` (this is the authoritative inventory of what to move).
  </read_first>
  <action>
For each partial-port archive `<slug>` in [aaro, nara, nasa, nz, uruguay]:
  1. `mkdir -p legacy/<slug>`
  2. For every git-tracked path under `<slug>/` (from `git ls-files <slug>/`):
     - If the path is exactly `<slug>/index.html` — DO NOT MOVE. Astro owns the index route via `src/pages/<slug>/index.astro`. Skip with a logged note.
     - Otherwise, run `git mv <slug>/<rel> legacy/<slug>/<rel>` (preserve relative subpath so `aaro/pages/faq.html` → `legacy/aaro/pages/faq.html`, `aaro/assets/images/Go_Fast_UAP.jpg` → `legacy/aaro/assets/images/Go_Fast_UAP.jpg`).
     - For nested directories, `git mv` handles the intermediate directory creation if you target a non-existent parent; if it errors, `mkdir -p legacy/<slug>/<parent_dir>` then re-run.
  3. After moving everything for that slug, the original `<slug>/` should contain ONLY `<slug>/index.html` (or be empty if a slug has no Astro-owned page — but for aaro/nara/nasa/nz/uruguay, index.html survives in the original location ONLY if it's git-tracked legacy HTML that the build pipeline ships). Confirm via `git ls-files <slug>/index.html`. **If it returns the file, the legacy `<slug>/index.html` IS a legacy file and goes into `legacy/<slug>/`** via `git mv <slug>/index.html legacy/<slug>/index.html`. The Astro-owned route at `src/pages/<slug>/index.astro` is untouched — Astro emits the index for `/<slug>/` at build time regardless. Plan 04.1-02 Task 1 step 3 updates `copy-legacy-archives.sh`'s skip rule from `case "$f" in <slug>/index.html) continue ;;` to `case "$f" in legacy/<slug>/index.html) continue ;;` so the move is safe (cross-reference: W-6 in 04.1-PLAN-CHECK.md).

Do NOT touch `src/pages/<slug>/index.astro` or any other file under `src/`.

After this task, `aaro/`, `nara/`, `nasa/`, `nz/`, `uruguay/` should either be empty directories (git auto-removes them on rename of last file) or fully gone. If empty directories linger in the working tree after the moves, leave them — git tracks files not dirs and they will disappear after commit.
  </action>
  <verify>
    <automated>test -f legacy/aaro/tic-tac.html && test -f legacy/aaro/pages/faq.html && test -f legacy/aaro/assets/favicon.svg && test -f legacy/nara/roswell.html && test -f legacy/nara/pages/blogs-and-articles.html && test -f legacy/nasa/story.html && test -f legacy/nz/kaikoura.html && test -f legacy/uruguay/story.html && [ "$(git ls-files aaro/ | grep -cv '^aaro/index.html$' || echo 0)" = "0" ] && [ "$(git ls-files nara/ | grep -cv '^nara/index.html$' || echo 0)" = "0" ] && [ "$(git ls-files nasa/ | grep -cv '^nasa/index.html$' || echo 0)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `legacy/aaro/tic-tac.html`, `legacy/aaro/pages/faq.html`, `legacy/aaro/assets/favicon.svg` exist.
    - `legacy/nara/roswell.html`, `legacy/nara/pages/blogs-and-articles.html` exist.
    - `legacy/nasa/story.html` exists.
    - `legacy/nz/kaikoura.html` exists.
    - `legacy/uruguay/story.html` exists.
    - `git ls-files aaro/` returns AT MOST `aaro/index.html` (or empty if that was also moved — both states acceptable; copy-legacy-archives.sh in plan 04.1-02 will be updated to read from `legacy/`).
    - `git ls-files nara/` returns AT MOST `nara/index.html`; same for nasa, nz, uruguay.
    - No file under `src/`, `public/` has been moved or modified.
  </acceptance_criteria>
  <done>aaro/nara/nasa/nz/uruguay partial-port legacy files live under legacy/<slug>/; root <slug>/ dirs are empty or contain only legacy index.html (acceptable — script update in 04.1-02 covers both cases).</done>
</task>

<task type="auto">
  <name>Task 3: Move dormant archive wholesale dirs into legacy/</name>
  <files>
    legacy/geipan/ (full contents),
    legacy/uk/ (full contents),
    legacy/brazil/ (full contents),
    legacy/chile/ (full contents),
    legacy/argentina/ (full contents),
    legacy/canada/ (full contents),
    legacy/italy/ (full contents),
    legacy/peru/ (full contents),
    legacy/spain/ (full contents)
  </files>
  <read_first>
    - /Users/laichan/code/war-gov-ufo-release/.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-CONTEXT.md (§ decisions "Legacy reorg layout" — dormant directories list)
    - /Users/laichan/code/war-gov-ufo-release/CLAUDE.md §2 (archive status table — 9 dormant archives confirmed)
    - /Users/laichan/code/war-gov-ufo-release/scripts/copy-legacy-archives.sh line 57 (current dormant-wholesale loop iterates exactly this 9-slug list)
  </read_first>
  <action>
For each dormant archive `<slug>` in [geipan, uk, brazil, chile, argentina, canada, italy, peru, spain]:
  1. `mkdir -p legacy/<slug>`
  2. For every git-tracked path under `<slug>/` (from `git ls-files <slug>/`):
     - `git mv <slug>/<rel> legacy/<slug>/<rel>` preserving relative subpath.
  3. Do NOT special-case `<slug>/index.html` for dormant archives — there is no Astro `src/pages/<slug>/index.astro` for any of the 9 dormant slugs, so the legacy index.html must move too.

Verify with `git ls-files <slug>/` returning empty after each archive.

Same exclusion rules as Task 2: do not touch `src/`, `public/`, `assets/`, `slideshow/`, `slideshow-2/`, `bundles/`, `data/`.
  </action>
  <verify>
    <automated>test -f legacy/geipan/index.html && test -f legacy/geipan/trans-en-provence.html && test -f legacy/uk/rendlesham.html && test -f legacy/brazil/varginha.html && test -f legacy/chile/el-bosque.html && test -f legacy/canada/falcon-lake.html && test -f legacy/italy/index.html && test -f legacy/peru/index.html && test -f legacy/spain/index.html && test -f legacy/argentina/index.html && for s in geipan uk brazil chile argentina canada italy peru spain; do [ "$(git ls-files $s/ | wc -l | tr -d ' ')" = "0" ] || { echo "$s still has tracked files"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `legacy/geipan/index.html`, `legacy/geipan/trans-en-provence.html` exist.
    - `legacy/uk/rendlesham.html`, `legacy/uk/cosford.html` exist.
    - `legacy/brazil/varginha.html`, `legacy/brazil/operacao-prato.html`, `legacy/brazil/trindade.html` exist.
    - `legacy/chile/el-bosque.html`, `legacy/canada/falcon-lake.html`, `legacy/canada/shag-harbour.html`, `legacy/italy/index.html`, `legacy/peru/index.html`, `legacy/spain/manises.html`, `legacy/argentina/index.html` all exist.
    - `git ls-files <slug>/` is empty for every dormant slug in [geipan, uk, brazil, chile, argentina, canada, italy, peru, spain].
    - `git status` shows rename entries (`R`) — not delete+add pairs.
  </acceptance_criteria>
  <done>All 9 dormant archive directories live entirely under legacy/<slug>/. Repo root dormant <slug>/ directories no longer exist in git's tracked-files view.</done>
</task>

<task type="auto">
  <name>Task 4: Extend scripts/snapshot-urls.py EXCLUDE_DIR_PREFIXES so legacy/ never leaks into URL-CONTRACT.txt</name>
  <files>scripts/snapshot-urls.py</files>
  <read_first>
    - /Users/laichan/code/war-gov-ufo-release/scripts/snapshot-urls.py (current 335-line version — lines 71-81 host EXCLUDE_DIR_PREFIXES; lines 134-137 host is_excluded() which checks the tuple)
    - /Users/laichan/code/war-gov-ufo-release/.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-PLAN-CHECK.md §3 B-2 (full rationale — without this fix, any future snapshot-urls.py run injects ~89 /legacy/* routes and clobbers Plan 04.1-06 hand-edits)
    - /Users/laichan/code/war-gov-ufo-release/URL-CONTRACT.txt (verify zero `/legacy/` entries pre-Task — sanity check that the snapshot pre-dates the move; Tasks 1-3 just moved files but URL-CONTRACT.txt was generated before the move)
  </read_first>
  <action>
Edit `scripts/snapshot-urls.py` in place. ONE change only:

1. Find the `EXCLUDE_DIR_PREFIXES` tuple at lines 71-81 (current contents: `'.git/', '.cache/', '.pdftext/', '__pycache__/', 'node_modules/', 'bundles/', 'assets/vendor/', '_site/'`).
2. Insert `'legacy/'` as a new entry. Alphabetical insertion is preferred (between `'bundles/'` and `'node_modules/'`) but any position inside the tuple is acceptable since `is_excluded()` uses `any()`.
3. Update the comment block above the tuple (line 70-71) to mention: `'legacy/'` is excluded post-Phase 04.1 — the legacy/ tree holds pre-SSG HTML that is NOT user-navigable from the active surface; its public URLs are surfaced indirectly via _redirects 301s (see Plan 04.1-06), not via direct enumeration.

Do NOT touch any other line in the file. Do NOT add a new `--stories` flag or extend the script to enumerate stories.json — that is Plan 04.1-06's concern (build-redirects.py reads JSON manifests; snapshot-urls.py stays scoped to filesystem HTML enumeration).

After the edit, run a smoke check:
```bash
python3 scripts/snapshot-urls.py --stdout | grep -c '^/legacy/'
# Expected output: 0
```

Do NOT re-write URL-CONTRACT.txt in this task; Plan 04.1-06 regenerates it from the (now-clean) snapshot output and merges in stories.json/site-pages.json canonicals via build-redirects.py.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; t=ast.parse(open('scripts/snapshot-urls.py').read()); ok=False
for n in ast.walk(t):
 if isinstance(n,ast.Assign) and len(n.targets)==1 and getattr(n.targets[0],'id','')=='EXCLUDE_DIR_PREFIXES':
  vals={e.value for e in n.value.elts if isinstance(e,ast.Constant)}
  ok = 'legacy/' in vals
sys.exit(0 if ok else 1)" && [ "$(python3 scripts/snapshot-urls.py --stdout | grep -c '^/legacy/' | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/snapshot-urls.py` `EXCLUDE_DIR_PREFIXES` tuple contains the literal `'legacy/'`.
    - `python3 scripts/snapshot-urls.py --stdout | grep -c '^/legacy/'` returns `0` (the post-move filesystem has tracked HTML under legacy/, but the script filters it out).
    - Comment block above the tuple references Phase 04.1 and the 301-redirect strategy.
    - No other line in the script changes (diff is ≤ 4 lines).
  </acceptance_criteria>
  <done>snapshot-urls.py emits zero /legacy/* routes even though legacy/ is full of tracked HTML; Plan 04.1-06 can safely regenerate URL-CONTRACT.txt without polluting it.</done>
</task>

<task type="auto">
  <name>Task 5: Inventory legacy HTML structural anchors so Plans 04.1-03 + 04.1-04 extractors have a documented selector cascade</name>
  <files>
    .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md
  </files>
  <read_first>
    - Output of `find legacy -name '*.html' -type f` (full inventory after Tasks 1-3)
    - /Users/laichan/code/war-gov-ufo-release/.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-PLAN-CHECK.md §3 B-3 (full rationale — pre-Phase-4 build-script HTML likely lacks <main>, and falling back to raw <body> would double-render Nav/Footer/scanlines)
    - One sample of each likely shape (sanity-check before generating the audit):
      - `legacy/aaro/tic-tac.html` — story sub-page from Phase 4-era build-aaro.py
      - `legacy/about.html` — root utility page
      - `legacy/map.html` — interactive page with body-bottom scripts
      - `legacy/geipan/index.html` — dormant archive index from older build script
  </read_first>
  <action>
Generate `.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md` by running grep against every file under `legacy/`. The audit is a documentation artifact consumed by Plan 04.1-03 Task 2 and Plan 04.1-04 Task 2 to drive the extractor's selector cascade.

Audit structure (write this file via the Write tool, NOT via heredoc):

```markdown
# Phase 04.1 — Legacy HTML structural anchor audit

**Generated:** {ISO date}
**Source:** find legacy -name '*.html' -type f after Plan 04.1-01 Tasks 1-3

## Anchor inventory

| File | Has `<main>` | Has `<article>` | Other anchor (documented selector) |
|---|---|---|---|
| legacy/about.html | yes/no | yes/no | e.g. `<div class="page-content">` or — |
| legacy/aaro/tic-tac.html | … | … | … |
| ... | | | |

## Selector cascade (extractor contract for Plans 04.1-03 + 04.1-04)

Plans 04.1-03 Task 2 (stories) and 04.1-04 Task 2 (site pages) MUST use this cascade in order, first match wins:

1. `<main\b[^>]*>([\s\S]*?)<\/main>` — preferred, fully encapsulates body content.
2. `<article\b[^>]*>([\s\S]*?)<\/article>` — fallback for files where build-script emitted article-wrapped story content without an outer `<main>`.
3. Documented per-shape selectors (list below — derived from this audit). Examples that MAY apply depending on the actual file shapes found:
   - `<div\s+class="container"\b[^>]*>([\s\S]*?)<\/div>` — pre-Phase-4 build-script convention (only if grep confirms it wraps content uniquely; if `<div class="container">` is also used in Nav/Footer chrome, do NOT use this selector — the chrome will leak).
   - `<section\s+class="hero"\b[\s\S]*?(?=<footer)` — concatenation of all sections between hero and footer (only if every audited file follows this exact shape).
4. **NEVER fall back to raw `<body>`.** Raw `<body>` extraction in pre-Phase-4 files double-renders Nav/Footer/scanlines because the legacy build pipeline embedded its own page chrome. If no selector matches, FAIL the build with a clear error pointing to the offending file: `Extractor: no recognised structural anchor in legacy/<path>. Add a selector to extractLegacyBody.ts or wrap the file's content in <main>.`

## Files needing pre-emptive wrapping (none expected — flag as a gap if found)

(If any file matches none of selectors 1-3, list it here. Plan 04.1-03 / 04.1-04 cannot extract from it cleanly. Resolution options: (a) extend the selector cascade with a 4th documented selector specific to that file's shape, OR (b) treat the file as out-of-scope for stories/site-page routes and rely on the legacy 301 redirect instead.)

## Script preservation whitelist (for Plan 04.1-04 preserveScripts=true entries)

Scripts inside extracted `<main>` content are preserved by default. For map.html and timeline.html which place interactive JS in body-bottom `<script>` blocks OUTSIDE the chosen anchor, `extractScripts(html)` extracts those scripts from the SAME anchor slice as `extractMain()` (NOT from the legacy `<head>` or from outside the anchor) — see W-4 in 04.1-PLAN-CHECK.md.

Whitelist of body-region scripts to preserve (audited per file):

| File | Inline scripts found in anchor slice | Inline scripts found in body-tail (after anchor) | Action |
|---|---|---|---|
| legacy/map.html | (audit fills) | (audit fills) | preserve if interactive map relies on body-tail script(s); else drop |
| legacy/timeline.html | (audit fills) | (audit fills) | preserve if interactive timeline relies on body-tail script(s); else drop |

For all OTHER legacy files (about.html, foia.html, glossary.html, whatsnew.html, every story HTML): scripts are SCRUBBED unconditionally per CLAUDE.md §9 trust boundary (Plans 04.1-03 + 04.1-04 enforce this).
```

Generation method:
1. For each file F under `legacy/`, run `grep -l '<main' F` and `grep -l '<article' F`. Record yes/no.
2. For files where both are "no", grep for likely alternative anchors: `<div class="container"`, `<div class="page-content"`, `<section class="hero"`, `<div id="content"`. Record what you find.
3. Tabulate. Write the audit. List 4-6 representative files per archive (not all 30+; representative sample is sufficient for the planner's purpose).

Coverage requirement: the audit MUST list at least one file from each of {about/foia/glossary/whatsnew, map, timeline, every featured story slug from stories.json once it exists, one dormant story like rendlesham}. Aim for ~15-20 file rows total.

The audit is read by Plans 04.1-03 and 04.1-04; their extractors implement the cascade described in §"Selector cascade" of this file.
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md && grep -q '## Anchor inventory' .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md && grep -q '## Selector cascade' .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md && grep -q 'NEVER fall back to raw' .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md && [ "$(grep -c '| legacy/' .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md)" -ge "10" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md` exists.
    - File contains `## Anchor inventory` and `## Selector cascade` sections.
    - File explicitly prohibits raw `<body>` fallback (contains the literal text "NEVER fall back to raw").
    - Inventory table has ≥ 10 rows (one row per audited file under `| legacy/...`).
    - At least one row covers each of: `legacy/about.html`, `legacy/aaro/tic-tac.html` (or another aaro story), `legacy/map.html`, `legacy/timeline.html`, `legacy/uk/rendlesham.html` (or another dormant-archive story), `legacy/geipan/trans-en-provence.html` (or any geipan file).
  </acceptance_criteria>
  <done>Audit document gives downstream extractor implementations (Plans 04.1-03 Task 2 + 04.1-04 Task 2) a documented selector cascade and a script-preservation whitelist; no guesswork left for the executor.</done>
</task>

</tasks>

## Verification

After all five tasks complete, run from repo root:

```bash
# (a) No HTML at repo root or in dormant-archive root dirs.
[ "$(ls *.html 2>/dev/null | wc -l | tr -d ' ')" = "0" ]
for s in geipan uk brazil chile argentina canada italy peru spain; do
  [ "$(git ls-files $s/ | wc -l | tr -d ' ')" = "0" ] || echo "FAIL: $s/ still tracked"
done

# (b) Partial-port archives: only Astro-owned index.html may remain at root <slug>/.
for s in aaro nara nasa nz uruguay; do
  bad=$(git ls-files $s/ | grep -v "^$s/index.html$" || true)
  [ -z "$bad" ] || echo "FAIL: $s/ has non-index files still: $bad"
done

# (c) legacy/ exists and is populated.
test -d legacy
[ "$(find legacy -type f | wc -l | tr -d ' ')" -gt "30" ]

# (d) Astro-owned + asset paths untouched (allow scripts/snapshot-urls.py edit from Task 4).
git status --porcelain | grep -E '^[^R].*(^src/|^public/|^assets/|^slideshow/|^slideshow-2/|^bundles/|^data/|^scripts/(?!snapshot-urls\.py)|astro.config|package.json|tsconfig)' && echo "FAIL: unexpected non-legacy files modified" || true

# (e) Snapshot-urls.py legacy exclusion in effect.
[ "$(python3 scripts/snapshot-urls.py --stdout | grep -c '^/legacy/' | tr -d ' ')" = "0" ] || echo "FAIL: snapshot-urls.py still emits /legacy/* routes"

# (f) Audit doc exists.
test -f .planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-legacy-html-structure-audit.md || echo "FAIL: audit missing"

# (g) pnpm build NOT expected to pass yet — copy-legacy-archives.sh still references old paths. That's fixed in 04.1-02.
```

## Threat model

| Boundary | Description |
|----------|-------------|
| working tree → git index | git mv is a tracked rename; surfaces in PR diff |
| filesystem walk → URL contract | snapshot-urls.py is_excluded() filter is the only thing keeping /legacy/* out of URL-CONTRACT.txt |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01-01 | Tampering | git mv vs cp+rm | mitigate | Use `git mv` (not `cp`+`rm`) so history is preserved as a rename. Verification asserts `^R` entries in `git status --porcelain`. |
| T-04.1-01-02 | Information Disclosure | accidental move of `data/` or `assets/` | mitigate | Task action enumerates an explicit allow-list (12 root .html files + 14 archive slugs); excludes `src/`, `public/`, `assets/`, `slideshow*/`, `bundles/`, `data/`. Verification asserts no non-legacy paths in `git status`. |
| T-04.1-01-03 | Denial of Service | empty `<slug>/` dirs left in working tree | accept | Empty dirs are untracked by git; they vanish after commit. No runtime impact. |
| T-04.1-01-04 | Tampering | snapshot-urls.py regression injecting /legacy/* | mitigate | Task 4 extends EXCLUDE_DIR_PREFIXES with `'legacy/'`; verification asserts `grep -c '^/legacy/'` returns 0 from `--stdout` output. Plan 04.1-06 build-redirects.py drift gate (--check) will fail loudly if this filter is later removed. |
| T-04.1-01-05 | Information Disclosure | audit doc reveals legacy structural patterns | accept | Audit is internal planning documentation; reveals public-domain HTML structure already visible in the repo. No secret. |
| T-04.1-01-SC | Tampering | npm/pnpm/python installs | accept | This plan installs no packages. Pure file move via shell + git + a 4-line edit to a stdlib-only python script + a markdown write. |
