---
phase: 04.1-legacy-reorg-stories-site-pages-nav-surface
plan: 02
subsystem: build-pipeline
tags:
  - build-pipeline
  - legacy-copy
  - postbuild
  - phase-04.1

requires:
  - phase: 04.1
    plan: 01
    provides: "`legacy/` directory populated with 138 tracked files — 12 root utility HTMLs, 5 partial-port slug subdirs (aaro/nara/nasa/nz/uruguay), 9 dormant slug wholesale dirs (geipan/uk/brazil/chile/argentina/canada/italy/peru/spain)."

provides:
  - "scripts/copy-legacy-archives.sh sources every legacy enumeration from `legacy/<slug>/` and writes to `dist/<slug>/` via guarded prefix-strip — URL contract preserved."
  - "W-2 guard implemented: `[[ \"$f\" == legacy/* ]]` branch strips `legacy/`; non-legacy paths (assets/, slideshow/, slideshow-2/, plus any future `assets/legacy-fonts/` or `legacy-snapshot/` directories) pass through verbatim."
  - "Top-level static-pages block REMOVED. search.html / stats.html / timeline.html / whatsnew.html will be Astro-served (04.1-04), parked-410 (04.1-06), or already-Astro (search.astro) — no longer copied from repo root (where they no longer live anyway, post-04.1-01)."
  - "Partial-port `case \"$f\" in <slug>/index.html) continue ;;` skip rule rewritten to `case \"$f\" in legacy/<slug>/index.html) continue ;;` for all 5 slugs (W-6 contract) — defense-in-depth against any future tracked legacy index.html shadowing Astro's index route."
  - "`pnpm build` end-to-end green: prebuild (normalize-csv) + astro build (7 prerendered routes including 5 partial-port + search + wargov index) + postbuild (184 files copied from legacy/ + 40 from slideshow/slideshow-2 + assets) + pagefind (1558 words indexed across 4 active archives). dist/ matches the pre-04.1 URL contract for every partial-port + dormant + asset path."

affects:
  - 04.1-03 (stories extractor enumerates from legacy/<slug>/<case>.html — same source paths copy-legacy-archives.sh now ingests)
  - 04.1-04 (site-pages extractor enumerates from legacy/<root-utility>.html — same root-utility paths Plan 04.1-01 moved)
  - 04.1-06 (URL contract regen reads dist/ and assumes /<slug>/, /<slug>/<case>.html land where copy-legacy-archives.sh now puts them)
  - 04.1-07 (verification smoke greps dist/ for the same paths this build smoke just confirmed exist)

tech-stack:
  added: []
  patterns:
    - "Guarded parameter expansion: `if [[ \"$f\" == legacy/* ]]; then dest=\"${f#legacy/}\"; else dest=\"$f\"; fi`. Replaces bare `${f#legacy/}` (which is a coincidence-no-op for non-`legacy/`-prefixed paths). The explicit test makes the contract audit-visible and immune to future directories that share `legacy` as a substring (e.g. `legacy-snapshot/`, `assets/legacy-fonts/`)."
    - "Defense-in-depth partial-port skip: `case \"$f\" in legacy/<slug>/index.html) continue ;;` for all 5 partial-port slugs, even though no such file is currently git-tracked. Mirrors the same hypothetical-but-documented W-6 contract that Plan 04.1-01 established when it kept the analogous skip rule in its `git ls-files`-based enumeration."

key-files:
  modified:
    - scripts/copy-legacy-archives.sh (100 insertions, 47 deletions — function rewrite + 6 loop-block path rewrites + 1 deleted block + 1 updated docstring + ~30 comment-block updates)

key-decisions:
  - "Used `pnpm build` (full pipeline) as the Task 2 verification harness, not a unit-test of copy_one() in isolation. Reason: the script's contract is end-to-end (`pnpm build && bash scripts/copy-legacy-archives.sh` MUST produce a deployable dist/ tree per the orchestrator brief). A pure shell-level test of `copy_one()` would have caught Task 1's W-2 guard regressions but not the integration points (Astro prerender + postbuild ordering + Pagefind invocation downstream). Plan-spec automated verify exit-0 = integration green."
  - "Did NOT regenerate Pagefind for the dormant archives. Pagefind still indexes only 4 pages (1558 words) because RootLayout still gates `data-pagefind-body` on `ACTIVE_ARCHIVES = {wargov, aaro, nasa, nara}` and emits `data-pagefind-ignore` on dormant `<main>` elements. This matches the pre-04.1 contract (the script-rewrite plan does not own RootLayout). The 04.1-PLAN-CHECK.md B-1 blocker (Pagefind invisibility for dormant-archive stories) lives in Plan 04.1-03 territory — explicit deferral, not silent scope drift."
  - "Verification-only Task 2 produced no committable artifacts (dist/ is .gitignored). Wave 2 commit count = Task 1 + SUMMARY metadata = 2 commits, matching the orchestrator brief."

requirements-completed:
  - SSG-10

# Metrics
duration: 4m 52s
duration_seconds: 292
started: 2026-06-01T19:32:45Z
completed: 2026-06-01T19:37:37Z
tasks_complete: 2
tasks_total: 2
files_modified: 1
---

# Phase 04.1 Plan 04.1-02: copy-legacy-archives.sh path rewrite Summary

**Rewrote `scripts/copy-legacy-archives.sh` to source every enumeration from `legacy/<slug>/` (Wave 1's relocation) with a W-2-guarded prefix-strip on write — `dist/<slug>/<file>` URL contract preserved verbatim — and dropped the now-dead top-level static-pages block; `pnpm build` produces a fully deployable dist/ tree with 22 of 22 spot-checked artifacts present and zero `/legacy/` href leakage.**

## Performance

- **Duration:** 4 min 52 sec (292 s)
- **Started:** 2026-06-01T19:32:45Z
- **Completed:** 2026-06-01T19:37:37Z
- **Tasks:** 2 / 2
- **Files modified:** 1 (`scripts/copy-legacy-archives.sh` — 100 insertions, 47 deletions)
- **Commits:** 1 (Task 1) + SUMMARY metadata commit = 2

## Accomplishments

- `scripts/copy-legacy-archives.sh` now reads from `legacy/<slug>/` for all 14 enumerated archives (9 dormant wholesale + 5 partial-port with index-skip) and writes to `dist/<slug>/` via `copy_one()`'s guarded prefix-strip — `legacy/aaro/tic-tac.html` lands at `dist/aaro/tic-tac.html`, not `dist/legacy/aaro/tic-tac.html`.
- W-2 plan-check guard implemented: `[[ "$f" == legacy/* ]]` test gates the prefix-strip so paths like `assets/legacy-fonts/x.woff` or a hypothetical `legacy-snapshot/x.html` pass through verbatim. Three-case behavioural smoke (legacy-prefix, substring, prefix-like) all PASS.
- W-6 plan-check defense-in-depth: every partial-port `case "$f" in <slug>/index.html) continue ;;` rewritten to `case "$f" in legacy/<slug>/index.html) continue ;;`. No tracked legacy index.html currently exists for any partial-port slug (Astro owns those routes via `src/pages/<slug>/index.astro`), but the rewrite makes the contract idempotent against future additions.
- Top-level static-pages block (`for f in search.html stats.html timeline.html whatsnew.html`) DELETED. `search.html` is already Astro-served (`src/pages/search.astro`); `timeline.html` + `whatsnew.html` get Astro routes in Plan 04.1-04; `stats.html` is parked-410 per Plan 04.1-06 + CONTEXT.md. All four files now live under `legacy/` (post-04.1-01) and are not copied to dist by this script.
- `assets/`, `slideshow/`, `slideshow-2/` root-level copies UNTOUCHED — CONTEXT.md decision ("KEEP at root: assets/, slideshow/, slideshow-2/, bundles/. They are NOT legacy."). `copy_one()`'s guard explicitly short-circuits the prefix-strip for these paths (`else dest="$f"`).
- Pagefind invocation, MAX_BYTES 25 MiB cap, `set -euo pipefail`, and `git ls-files`-based enumeration all PRESERVED verbatim.
- End-to-end `pnpm build` green: 7 Astro prerender routes + 184 legacy files copied + 40 slideshow JPGs + assets + Pagefind index (4 pages, 1558 words). Zero `/legacy/` path leakage in dist tree; zero `/legacy/` hrefs in any built HTML.

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite `copy-legacy-archives.sh`** — `dcc31f3` (build: 100 insertions, 47 deletions)
2. **Task 2: `pnpm build` smoke verification** — no commit (verification-only; dist/ is .gitignored)

Final metadata commit follows this SUMMARY (orchestrator-owned).

## Files Created/Modified

**Modified:**
- `scripts/copy-legacy-archives.sh` — 100 insertions, 47 deletions; rewrote `copy_one()` with W-2 guard; rewrote 1 dormant-wholesale loop + 5 partial-port blocks to source from `legacy/<slug>/`; rewrote the 5 partial-port case-skip rules to match `legacy/<slug>/index.html`; deleted the top-level static-pages block; updated header docstring + ~30 inline comment blocks to reflect the legacy/ source contract.

**Not committed (dist/ is .gitignored):**
- 184 files copied from `legacy/<slug>/` into `dist/<slug>/` by the rewritten script (verified via 22 spot-check `test -f` assertions).
- 30 slideshow JPGs + 10 slideshow-2 JPGs + assets — unchanged behaviour.

## Decisions Made

- **Used `pnpm build` (full pipeline) for Task 2 verification, not an isolated `copy_one()` unit test.** Reason: the script's contract is end-to-end (`pnpm build && bash scripts/copy-legacy-archives.sh` MUST produce a deployable dist/ per the orchestrator brief). A shell-level mock of `copy_one()` would have validated the W-2 guard logic in isolation (which the Task 1 verify already does) but not the integration points: Astro prerender ordering, postbuild trigger, Pagefind invocation, MAX_BYTES enforcement, and the 14 enumerated `if [ -d ]` guards. Running the full pipeline catches the entire contract surface in one shot.

- **Deferred RootLayout Pagefind-marker fix to Plan 04.1-03.** 04.1-PLAN-CHECK.md §3 B-1 identifies that dormant-archive stories (rendlesham, trans-en-provence, varginha, falcon-lake) are invisible to Pagefind because `RootLayout` emits `data-pagefind-ignore` on dormant `<main>` elements. This plan does not own RootLayout; the fix belongs in Plan 04.1-03 (stories extractor) where the new `/stories/<slug>/` route can opt in via a `forceIndexing` prop OR via a refactored `pagefindMode` prop. Pagefind currently indexes 4 pages (active archives) — same as pre-04.1. No regression.

- **Verification-only Task 2 produces no committable artifacts.** dist/ is .gitignored. Wave 2 commit count is therefore Task 1 + the SUMMARY metadata commit = 2 commits, matching the orchestrator brief's "2 commits expected".

## Deviations from Plan

None — plan executed exactly as written.

The plan's Task 1 action prescribed the W-2 guard, the 6 path rewrites, the static-pages-block deletion, and the preservation rules for `copy_one()`'s helper / Pagefind block / MAX_BYTES cap / `set -euo pipefail` / `git ls-files` enumeration. All implemented verbatim. Task 1 automated verify (8 assertions including 3-case W-2 behavioural smoke) and Task 2 automated verify (23-path existence + 2 anti-pattern grep) both PASS.

## Issues Encountered

- **Worktree-branch base point predated Wave 1.** Spawn-time `git log` on this worktree branch showed `c879473` (pre-04.1) as HEAD; Wave 1's 5 commits had been merged to `main` since the worktree was created. Resolved by `git merge --ff-only main`, a pure fast-forward (zero local commits on the worktree branch, `c879473` is an ancestor of `main`). Post-merge: `legacy/` populated, 138 files tracked under `legacy/<slug>/`, ready for Wave 2. Non-destructive (no force, no rebase, no reset).

- **CF Pages 25 MiB cap not exercised.** No file in the current legacy/<slug>/ population exceeds 25 MiB, so `skipped_count` stays at 0. The cap-check code is preserved verbatim from the prior version — if Plan 04.1-03's stories generator or any future plan adds a >25 MiB file, the existing skip logic still fires correctly.

## User Setup Required

None — pure script rewrite + `pnpm install` (one-time per fresh worktree) + `pnpm build`. No new dependencies, no env vars, no external services.

## Next Phase Readiness

**Ready for Plan 04.1-03 (stories extractor):**
- `legacy/<slug>/<case>.html` paths are the source for `getStaticPaths()` enumeration. This plan does NOT alter the legacy file inventory; it only adjusts how `copy-legacy-archives.sh` enumerates them. Plans 04.1-03's extractor can pull from the same paths.
- The dormant-archive stories (rendlesham, trans-en-provence, varginha, falcon-lake) still ship as legacy HTML at `dist/<slug>/<case>.html` — Plan 04.1-03 will additionally surface them under `/stories/<slug>/` via Astro routes and emit 301 redirects to the new canonical via Plan 04.1-06.

**Ready for Plan 04.1-04 (site-pages extractor):**
- `legacy/about.html`, `legacy/foia.html`, `legacy/glossary.html`, `legacy/map.html`, `legacy/timeline.html`, `legacy/whatsnew.html` all reachable on disk. The top-level static-pages copy-block this plan deleted was redundant the moment Plan 04.1-04 lands its Astro routes; deleting now (rather than waiting) keeps dist/ from carrying stale `.html` files alongside the new `/about/`, `/foia/`, etc. routes.

**Ready for Plan 04.1-06 (URL contract regeneration + redirects):**
- `dist/<slug>/index.html` and `dist/<slug>/<case>.html` paths confirmed live after `pnpm build`. Plan 04.1-06 can regenerate `URL-CONTRACT.txt` against the dist/ tree and emit `_redirects` rules confident the canonical paths land where the redirects target them.

**Ready for Plan 04.1-07 (verification smoke):**
- The 22-path spot-check in Task 2's verify is the same shape Plan 04.1-07 will run at viewport breakpoints — the dist/ tree this build smoke confirmed is the same input.

**Blockers:** None.

## Threat Flags

None. This plan only modifies an internal build script; no new network endpoints, auth paths, file access patterns, or schema changes. The threat register in PLAN.md (T-04.1-02-01..03 + T-04.1-02-SC) is unchanged and accurate: T-04.1-02-01 mitigated by W-2 guard + 3-case behavioural smoke (PASS), T-04.1-02-02 mitigated by Astro re-routing of removed paths (Plans 04.1-04 + 04.1-06 own the routing), T-04.1-02-03 accept (Pagefind input shape unchanged), T-04.1-02-SC accept (no installs).

## Self-Check

Verified before completion:

- `scripts/copy-legacy-archives.sh` syntax check `bash -n` ✓
- `grep -c 'git ls-files "legacy/'` returns 6 (>= 6) ✓
- `for f in search.html stats.html timeline.html whatsnew.html` block deleted ✓
- Guarded prefix-strip `[[ "$f" == legacy/* ]]` present in `copy_one()` ✓
- `${f#legacy/}` present inside guard branch ✓
- Trailing `pnpm exec pagefind --site dist` preserved verbatim ✓
- W-2 3-case behavioural smoke: legacy/aaro/x.html→aaro/x.html ✓; assets/legacy-fonts/x.woff→unchanged ✓; legacy-snapshot/x.html→unchanged ✓
- `pnpm build` exits 0 ✓
- 22-path Task 2 acceptance check: all artifacts present ✓
- `find dist -name '*.html' -path '*/legacy/*'` zero matches ✓
- `grep -rIl '/legacy/' dist/aaro/ dist/nara/ dist/nasa/ dist/geipan/` zero matches ✓
- `dist/aaro/index.html` contains "All-domain Anomaly Resolution Office" (Astro hero verbatim) ✓
- `dist/aaro/tic-tac.html` contains "Nimitz" ✓
- `dist/geipan/index.html` contains "GEIPAN" ✓
- `dist/slideshow/` has 30 files; `dist/slideshow-2/` has 10 files ✓
- Task 1 commit `dcc31f3` reachable from HEAD via `git log` ✓

## Self-Check: PASSED

Verified post-write:

- SUMMARY.md exists at `.planning/phases/04.1-legacy-reorg-stories-site-pages-nav-surface/04.1-02-SUMMARY.md` ✓
- Task 1 commit hash `dcc31f3` resolves in `git log --oneline` ✓
- scripts/copy-legacy-archives.sh modified file confirmed in commit ✓

---

*Phase: 04.1-legacy-reorg-stories-site-pages-nav-surface*
*Completed: 2026-06-01*
