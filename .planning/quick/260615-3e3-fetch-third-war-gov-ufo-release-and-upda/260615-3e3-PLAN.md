---
phase: quick-260615-3e3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/download-war.gov.py
  - uap-data.csv
  - data/wargov.json
  - data/wargov-shard-2.json
  - data/wargov-shard-3.json
  - data/wargov-shard-4.json
  - data/wargov-shard-5.json
  - public/data/wargov.json
  - scripts/normalize-csv.py
  - src/components/Card.astro
  - src/pages/index.astro
autonomous: false
requirements: [SRC-wargov-r03]
must_haves:
  truths:
    - "Re-fetching uap-data.csv from war.gov and diffing reveals the Release-03 rows (a Release Date value other than 5/8/26 / 5/22/26)"
    - "normalize-csv.py re-runs clean, emitting Release-03 rows into data/wargov.json + shards with --check passing"
    - "New Release-03 PDF/VID/AUD assets resolve to durable URLs (R2 custom-domain via rewrite_to_r2, fed by a wargov-r03-v1 GitHub Release tag) — never bare local paths"
    - "pnpm build succeeds and the wargov landing page + Pagefind index include the new Release-03 records"
    - "The release-batch filter recognises Release 03 (Card.astro releaseBatch() and normalize-csv.py render_card_html stay byte-equivalent per D-10)"
  artifacts:
    - path: "uap-data.csv"
      provides: "Freshly-downloaded combined master CSV including Release-03 rows"
    - path: "data/wargov.json"
      provides: "Normalised wargov envelope with Release-03 rows"
    - path: "scripts/download-war.gov.py"
      provides: "Release-03 slideshow + bundle entries (idempotent)"
  key_links:
    - from: "uap-data.csv"
      to: "data/wargov.json"
      via: "python3 scripts/normalize-csv.py"
      pattern: "normalize-csv"
    - from: "data/wargov.json"
      to: "dist/ + Pagefind"
      via: "pnpm build"
      pattern: "pnpm build"
---

<objective>
Fetch the third war.gov UAP release ("Release 03" / third patch) and wire its new
records + assets into realufo.org's wargov archive.

Purpose: Keep the faithful offline archive current with the official war.gov/UFO
source as new tranches drop (CLAUDE.md Goal #1).

Output: Updated uap-data.csv (re-downloaded verbatim), Release-03 entries in
download-war.gov.py, regenerated data/wargov.json + shards, release-batch filter
recognising Release 03, new wargov-r03-v1 GitHub Release tag, and a rebuilt dist/
whose landing page + Pagefind index surface the new records.

CRITICAL — DISCOVERY-FIRST: This plan does NOT assume Release 03 exists at a
hardcoded URL. Task 1 re-fetches uap-data.csv and diffs it to PROVE the third
patch is live and enumerate its rows/assets. If the diff shows no new Release Date
value, STOP and report "no third release detected" — do not fabricate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@scripts/download-war.gov.py
@scripts/normalize-csv.py
@scripts/sync.sh
@src/content.config.ts

<interfaces>
<!-- Ground truth extracted from the codebase — executor should NOT re-explore. -->

CURRENT STATE (verified 2026-06-15):
- uap-data.csv: 222 non-empty-Title rows. Release Date distribution:
    158 rows = "5/8/26"  (Release 01)
     64 rows = "5/22/26" (Release 02)
  A THIRD distinct Release Date value appearing after re-download == Release 03.
- CSV header (col order, literal spaces, trailing-comma sentinel cols intact):
    Redaction,Release Date,Title,Type,Video Pairing,PDF Pairing,Description Blurb,
    DVIDS Video ID,Video Title,Agency,Incident Date,Incident Location,
    PDF | Image Link,Modal Image,Image Alt Text,Image VIRIN,,,,,,,,,,,
- Existing GitHub Release tags (convention): wargov-r02-v1 (per-release), plus
  shared pdfs-v1 / videos-v1. Release 03 tag SHOULD be `wargov-r03-v1`.
- Asset URL contract: normalize-csv.py `_read_rows` rewrites PDF/DOC/VID/AUD
  `PDF | Image Link` to R2 custom-domain via `_archive_common.rewrite_to_r2`
  → https://assets.realufo.org/{pdfs|videos}/wargov/<basename>. VID/AUD also
  hydrate from scripts/dvids2dod-r0{1,2}.json (DVIDS catalog→DOD asset map).
  GitHub Release tags are the durable upload target; R2 mirrors them. NEVER a
  bare local path on the deployed site (CLAUDE.md §4.3 + §5.1).

RELEASE-BATCH MAPPING IS DUPLICATED (must update BOTH — D-10 byte-equivalence):
- src/components/Card.astro  ~L80-83  releaseBatch():
      if (releaseDate === '5/8/26') return '01';
      if (releaseDate === '5/22/26') return '02';
- scripts/normalize-csv.py  ~L573-579  render_card_html():
      if release_date == '5/8/26':   release = '01'
      elif release_date == '5/22/26': release = '02'
      else: release = ''
  These two MUST stay textually parallel or Plan 03-05's markup-contract
  verification breaks. Add the Release-03 date → '03' branch to BOTH.

UI COPY referencing the current 2-release / 222-record state (src/pages/index.astro):
- L97  SEO `description` ("Release 01 ... + Release 02 ... 222-record archive ...")
- L111 JSON-LD description ("Releases 01 ... and 02 ... 222 declassified ...")
- L114 keywords array ('Release 01','Release 02')
- L196-215 hero head-cards: Release 01 / Release 02 / "222 unresolved records"
- L286-296 release filter <select> (#filter-release): <option> 01 + 02 only
- L681-684 page-local filter script reads data-release (no change needed —
  it already matches any value; only the <option> list must gain Release 03)

DOWNLOADER STRUCTURE (scripts/download-war.gov.py):
- SLIDESHOW_BASE  = .../UFO/Slideshow/    + SLIDES[]   (R01, 17 imgs)
- SLIDESHOW_2_BASE= .../UFO/Slideshow-2/  + SLIDES_2[] (R02, 10 imgs)
  → Release 03 likely adds .../UFO/Slideshow-3/ — DISCOVER actual filenames.
- BUNDLES[] holds (url, filename) tuples. R02 pattern:
    medialink/ufo/052226/release_02/release_02_document_bundle.zip
    + cloudfront d34w7g4gy10iej.cloudfront.net/uap052226.zip
  → R03 follows an analogous medialink/ufo/<date>/release_03/... path —
    DISCOVER the exact URL from the war.gov/UFO/ page; do NOT hardcode a guess.
- fetch() is idempotent (skips on-disk non-empty files, multi-Chrome-impersonate
  + Wayback fallback). MANIFESTS[] already re-downloads uap-data.csv.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-fetch uap-data.csv and DIFF to discover Release 03 (gate)</name>
  <files>uap-data.csv</files>
  <action>
Re-download the combined master CSV from war.gov, which is the sanctioned update
path (CLAUDE.md §11 forbids HAND-editing the CSV; re-fetching the official file is
explicitly allowed and is the whole point of this task).

1. Snapshot the current Release-Date fingerprint BEFORE fetching:
   `python3 -c "import csv,collections; c=collections.Counter((r.get('Release Date') or '').strip() for r in csv.DictReader(open('uap-data.csv',encoding='utf-8-sig',newline='')) if (r.get('Title') or '').strip()); print(sorted(c.items()))"`
   Expected baseline: [('5/22/26', 64), ('5/8/26', 158)] (222 rows total).

2. Force a fresh download (the fetch() cache-skip would otherwise no-op). Move the
   current CSV aside first: `mv uap-data.csv uap-data.csv.prev`, then run
   `python3 scripts/download-war.gov.py` (it re-fetches MANIFESTS incl. uap-data.csv
   via curl_cffi Chrome impersonation + Wayback fallback). If the downloader cannot
   reach war.gov (Akamai/network), restore `mv uap-data.csv.prev uap-data.csv` and
   STOP with a clear network-failure report — do NOT fabricate rows.

3. DIFF the new Release-Date fingerprint against the baseline (re-run the snapshot
   command). Determine whether a NEW Release Date value (anything other than
   '5/8/26' / '5/22/26') is present.
   - If a new value EXISTS: record it (e.g. the Release-03 date string), the count
     of new rows, and enumerate the new rows' Type / Title / `PDF | Image Link` /
     `DVIDS Video ID` columns. Remove the `.prev` backup. Proceed to Task 2.
   - If NO new value: restore `mv uap-data.csv.prev uap-data.csv`, ABORT the plan,
     and report "no third release detected — uap-data.csv unchanged, third patch
     not yet live on war.gov." Do NOT continue to later tasks.

4. Confirm the re-downloaded CSV still parses with the existing header (col order +
   trailing-comma sentinel columns intact) so normalize-csv.py's DictReader path
   holds. If the header drifted, note the new/renamed columns for Task 3 (the
   content.config.ts Zod schema uses .strict()-adjacent literal keys and will
   hard-fail the build on an unknown column — flag it rather than papering over).
  </action>
  <verify>
    <automated>python3 -c "import csv,collections; c=collections.Counter((r.get('Release Date') or '').strip() for r in csv.DictReader(open('uap-data.csv',encoding='utf-8-sig',newline='')) if (r.get('Title') or '').strip()); new=[k for k in c if k not in ('5/8/26','5/22/26')]; print('RELEASE-03 DATES:', new, 'COUNTS:', {k:c[k] for k in new}); assert new, 'NO THIRD RELEASE DETECTED — abort plan'"</automated>
  </verify>
  <done>uap-data.csv re-downloaded verbatim from war.gov; diff confirms ≥1 new Release Date value with N new rows enumerated (Type/Title/asset-link/DVIDS recorded). If no new rows, plan aborted with "no third release detected".</done>
</task>

<task type="auto">
  <name>Task 2: Add Release-03 downloader entries (slideshow + bundle) idempotently</name>
  <files>scripts/download-war.gov.py</files>
  <action>
Using the Release-03 rows enumerated in Task 1, DISCOVER and wire the new asset
sources into the downloader so a fresh clone reproduces them (CLAUDE.md Goal #5).

1. Discover the Release-03 slideshow base + filenames: war.gov uses
   .../UFO/Slideshow/ (R01) and .../UFO/Slideshow-2/ (R02), so R03 most likely uses
   .../UFO/Slideshow-3/. Confirm the actual directory + image filenames from the
   Release-03 rows' `Modal Image` / `Image VIRIN` columns and the live war.gov/UFO/
   page markup. Add a `SLIDESHOW_3_BASE` constant + `SLIDES_3 = [...]` list and a
   `SLIDES_3_DIR = ROOT / "slideshow-3"` (mkdir alongside the others). Mirror the
   R02 `main()` block that fetches SLIDES_2 (URL-encode spaces with .replace(" ","%20")).
   If Release 03 contains NO new slideshow imagery, skip this sub-step and note it.

2. Discover the Release-03 document/video bundle URL(s): R02 used
   medialink/ufo/052226/release_02/release_02_document_bundle.zip + a cloudfront
   uap052226.zip. Derive the analogous medialink/ufo/<date>/release_03/... path
   from the Release-03 date and confirm it resolves (do NOT hardcode a guessed URL
   that 404s — verify via the downloader or war.gov/UFO/ page links). Append the
   discovered (url, filename) tuple(s) to the BUNDLES[] list with a `# Release 03`
   comment. Update the BUNDLES print banner in main() to mention Release 03.

3. Keep every addition idempotent: rely on the existing fetch() cache-skip + the
   per-Chrome-profile + Wayback fallback. Do NOT alter fetch()'s logic.

4. Re-run `python3 scripts/download-war.gov.py` to pull the newly-wired Release-03
   slideshow images + bundle into slideshow-3/ and bundles/. (uap-data.csv already
   on disk from Task 1 — fetch() will skip it.) If a discovered URL FAILs, record
   the failure but do not block — note it for the human-verify checkpoint (Task 5).
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; src=open('scripts/download-war.gov.py').read(); assert 'release_03' in src.lower() or 'Slideshow-3' in src or 'slideshow-3' in src, 'no Release-03 downloader entries found'; ast.parse(src); print('download-war.gov.py: Release-03 entries present + parses')"</automated>
  </verify>
  <done>download-war.gov.py has Release-03 slideshow and/or bundle entries (whichever the diff revealed), parses cleanly, re-runs idempotently. Discovered Release-03 assets present on disk (slideshow-3/ images + bundles/ zip), or any fetch failures recorded for Task 5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalise CSV + extend release-batch mapping (Card.astro + normaliser parity)</name>
  <files>scripts/normalize-csv.py, src/components/Card.astro, data/wargov.json, public/data/wargov.json</files>
  <behavior>
    - normalize-csv.py emits rows for the Release-03 date with data-release="03"
    - Card.astro releaseBatch() returns '03' for the Release-03 date string
    - Both mappings stay textually parallel (D-10 byte-equivalent contract)
    - VID/AUD Release-03 rows with a DVIDS Video ID hydrate to an R2 mp4 URL;
      PDF/DOC rows rewrite `PDF | Image Link` to assets.realufo.org/pdfs/wargov/<basename>
    - normalize-csv.py --check exits 0 after the write (no drift)
    - _assert_csv_unchanged passes (uap-data.csv NOT mutated)
  </behavior>
  <action>
1. Add the Release-03 date → '03' branch to BOTH duplicated mappings, keeping them
   textually parallel (this is the D-10 byte-equivalence contract Plan 03-05 verifies):
   - scripts/normalize-csv.py `render_card_html` (~L573): add
     `elif release_date == '<R03-DATE>': release = '03'` before the `else`.
   - src/components/Card.astro `releaseBatch()` (~L82): add
     `if (releaseDate === '<R03-DATE>') return '03';` before the existing returns'
     final fallthrough. Use the EXACT Release-03 date string from Task 1.

2. If Task 1 flagged any NEW/renamed CSV column, add the corresponding lenient
   `z.string().default('')` field to `wargovRowSchema` in src/content.config.ts so
   the build does not hard-fail on the unknown column (D-02 drift signal). If no new
   columns, skip — do NOT touch content.config.ts.

3. Run `python3 scripts/normalize-csv.py` to regenerate data/wargov.json +
   data/wargov-shard-*.json + their public/data/ mirrors. The Release-03 VID/AUD
   rows hydrate via scripts/dvids2dod-r0{1,2}.json when their DVIDS IDs are present;
   if Release-03 VID rows have DVIDS IDs NOT in those maps, note that the Open/Play
   button will be absent until a resolve-dvids run covers them (flag for Task 5 —
   do NOT fabricate a mapping). PDF/DOC asset links auto-rewrite to R2.

4. Do NOT add or modify uap-data.csv content — the normaliser is read-only and
   _assert_csv_unchanged enforces it. New shard files (if row count crosses a 50-card
   boundary) are expected; orphan-shard cleanup is automatic.
  </action>
  <verify>
    <automated>python3 scripts/normalize-csv.py && python3 scripts/normalize-csv.py --check && python3 -c "import json; d=json.load(open('data/wargov.json')); rows=d['v1']['rows']; print('primary rows:', len(rows), 'shards:', len(d['v1']['shards']))" && grep -q "return '03'" src/components/Card.astro && grep -q "release = '03'" scripts/normalize-csv.py && echo "release-03 mapping parity OK"</automated>
  </verify>
  <done>normalize-csv.py regenerates data/wargov.json + shards (+ public mirror) with Release-03 rows; `--check` exits 0; Card.astro and normalize-csv.py both map the Release-03 date to '03'; uap-data.csv unchanged (git diff clean). Any unresolved Release-03 DVIDS IDs noted for Task 5.</done>
</task>

<task type="auto">
  <name>Task 4: Update landing-page copy + filter, build, verify Pagefind picks up Release 03</name>
  <files>src/pages/index.astro</files>
  <action>
1. Update the Release-03-aware UI copy in src/pages/index.astro using the actual
   Release-03 date + new total row count from Task 1/3:
   - L286-296 release filter <select id="filter-release">: add
     `<option value="03">Release 03 (<R03 human date>)</option>`. (The L681-684
     filter script already matches any data-release value — no script change.)
   - L196-215 hero head-cards: add a "Release 03" head-card mirroring the Release-01
     / Release-02 cards, with a verbatim/substantive summary of the new tranche
     (CLAUDE.md §9 — no filler; distil the actual Release-03 row content). Update the
     "222 unresolved records" head-card (~L214) to the new total.
   - L97 SEO `description`, L111 JSON-LD description, L114 keywords: extend to mention
     Release 03 + the new record total (replace the hardcoded "222").
   Keep all copy faithful to the official source (CLAUDE.md §9).

2. Rebuild the full site: `pnpm build`. This runs the prebuild normaliser, Astro
   builds the 4 active archives, postbuild copies dormant archives, and Pagefind
   indexes dist/. The build MUST succeed (content.config.ts Zod validates wargov.json;
   a schema/CSV drift would hard-fail here).

3. Verify the Release-03 records are in the built output AND the Pagefind index:
   confirm dist/index.html (or dist/data/wargov*.json) contains data-release="03"
   cards, and that the Pagefind index under dist/pagefind/ regenerated (newer mtime,
   non-empty). Spot-check one Release-03 record title appears in a Pagefind fragment.
  </action>
  <verify>
    <automated>pnpm build && grep -rq 'data-release="03"' dist/index.html dist/data/wargov*.json 2>/dev/null && test -d dist/pagefind && grep -rqi 'release 03' dist/index.html && echo "BUILD OK + Release-03 in dist + Pagefind present"</automated>
  </verify>
  <done>pnpm build succeeds; dist/ contains Release-03 cards (data-release="03"); the release filter offers Release 03; hero carousel + SEO copy reference Release 03 and the new total; dist/pagefind/ regenerated and indexes the new records.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Fetched war.gov Release 03 (third patch): re-downloaded uap-data.csv verbatim,
wired new slideshow + bundle assets into download-war.gov.py, regenerated
data/wargov.json + shards, extended the release-batch filter to Release 03, updated
landing-page copy, and rebuilt dist/ with Pagefind. New Release-03 PDF/VID/AUD assets
point at R2 (assets.realufo.org), fed from the wargov-r03-v1 GitHub Release tag.
  </what-built>
  <how-to-verify>
1. Preview the build locally: `pnpm preview` (or `pnpm dev`) and open the wargov
   landing page (http://localhost:4321/ or the dev URL).
2. Confirm the hero carousel shows a Release 03 head-card and the record total
   updated from 222 to the new count.
3. In the archive browser, set the Release filter to "Release 03" — confirm ONLY
   the new Release-03 cards show, each with correct Title/Agency/Date and a working
   Open/Download (PDF iframe or new-tab) and Source ↗ button. For VID/AUD cards,
   confirm Play works OR (if any DVIDS IDs were unresolved in Task 3) note which
   cards lack a Play button — those need a resolve-dvids-r03 run before release.
4. Test the search box for a Release-03 record title — confirm Pagefind returns it.
5. Test at 360 px width — no horizontal scroll, single-column cards (CLAUDE.md §8).
6. UPLOAD the new Release-03 binaries to GitHub Releases under tag `wargov-r03-v1`
   (matching the wargov-r02-v1 convention) and ensure the R2 mirror
   (assets.realufo.org/{pdfs,videos}/wargov/) is populated so the deployed
   Open/Download buttons resolve — NEVER a bare local path (CLAUDE.md §4.3/§5.1).
   Wait for each upload to finish before the next (CLAUDE.md §11 don't).
  </how-to-verify>
  <resume-signal>Type "approved" once Release 03 renders correctly, assets resolve via R2/GitHub Release, and Pagefind finds the new records — or describe issues (e.g. unresolved DVIDS IDs, 404 bundle URL) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| war.gov → local repo | Akamai-protected remote source; CSV + binaries cross into the repo via curl_cffi |
| CSV → committed JSON | Untrusted free-text row fields rendered into HTML strings (shard cards) |
| GitHub Release / R2 → deployed site | Asset URLs the deployed page links to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3e3-01 | Tampering | uap-data.csv (re-download) | mitigate | normalize-csv.py `_assert_csv_unchanged` runs post-write; CSV opened read-only; re-download is the sanctioned update path (CLAUDE.md §11) |
| T-3e3-02 | Information disclosure | Release-03 row text → shard HTML | mitigate | `_e()` (html.escape quote=True) pipes every field before interpolation (existing T-03-25 mitigation, unchanged) |
| T-3e3-03 | Tampering | Card.astro / normaliser release-batch drift | mitigate | Task 3 updates BOTH mappings in parallel; verify greps both; Plan 03-05 markup-contract verification still applies |
| T-3e3-04 | Denial of service | broken asset URL on deployed site (404) | mitigate | Task 5 checkpoint requires R2/GitHub-Release upload + Open/Download verification before approval; rewrite_to_r2 never emits a bare local path |
| T-3e3-SC | Tampering | curl_cffi (already-installed dependency) | accept | No NEW package installs in this plan; curl_cffi pre-existing + audited; Wayback fallback bounded to war.gov host |
</threat_model>

<verification>
- Task 1 diff PROVES a new Release Date value exists (else plan aborts cleanly).
- `python3 scripts/normalize-csv.py --check` exits 0 (idempotent, no drift).
- `git diff --quiet -- uap-data.csv uap-release001.csv` (CSV untouched, §11).
- `grep "return '03'" src/components/Card.astro` AND `grep "release = '03'" scripts/normalize-csv.py` (mapping parity).
- `pnpm build` succeeds; dist/index.html contains `data-release="03"` cards.
- dist/pagefind/ regenerated; a Release-03 title is findable in search.
- 360 px mobile check (CLAUDE.md §8): no horizontal scroll, single-column grid.
</verification>

<success_criteria>
- uap-data.csv re-downloaded verbatim from war.gov; Release-03 rows present (or plan aborted "no third release detected").
- download-war.gov.py reproduces Release-03 slideshow + bundle assets idempotently.
- data/wargov.json + shards (+ public mirror) regenerated with Release-03 rows; --check clean; CSV unchanged.
- Release-batch filter, hero carousel, and SEO copy recognise Release 03 with the new record total.
- pnpm build succeeds; Pagefind index + landing page surface the new records.
- New Release-03 PDF/VID/AUD assets resolve via R2 (fed by wargov-r03-v1 GitHub Release) — never bare local paths.
</success_criteria>

<output>
Create `.planning/quick/260615-3e3-fetch-third-war-gov-ufo-release-and-upda/260615-3e3-SUMMARY.md` when done.
</output>
