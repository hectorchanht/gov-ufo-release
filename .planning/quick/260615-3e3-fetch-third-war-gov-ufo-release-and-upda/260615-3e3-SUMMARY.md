---
phase: quick-260615-3e3
plan: 01
subsystem: wargov-archive
tags: [wargov, release-03, csv-normalise, pagefind, downloader]
requires:
  - "uap-data.csv (re-fetched verbatim from war.gov)"
provides:
  - "Release-03 records (6/12/26, 72 rows) wired into wargov archive"
  - "data/wargov.json + shards regenerated to 294 rows"
  - "Release-batch filter + landing-page copy recognising Release 03"
affects:
  - scripts/download-war.gov.py
  - scripts/normalize-csv.py
  - src/components/Card.astro
  - src/content.config.ts
  - src/pages/index.astro
  - data/wargov.json
tech-stack:
  added: []
  patterns: ["curl_cffi TLS-impersonation discovery fetch", "D-10 byte-parallel release-batch mapping", "R2 rewrite_to_r2 asset URLs"]
key-files:
  created:
    - "slideshow-3/ (10 Release-03 Rotator images)"
    - "data/wargov-shard-6.json + public mirror"
  modified:
    - scripts/download-war.gov.py
    - scripts/normalize-csv.py
    - src/components/Card.astro
    - src/content.config.ts
    - src/pages/index.astro
    - uap-data.csv
    - data/wargov.json (+ shards 2-6, public mirror)
decisions:
  - "Release 03 imagery lives under /061226/Rotator/ (NOT a Slideshow-3/ dir); bundle is release_03_documents.zip (not the guessed release_03_document_bundle.zip)"
  - "New leading CSV column 'Featured' documented as lenient z.string() in content.config.ts (D-02 drift signal)"
  - "9 Release-03 VID/AUD DVIDS IDs left unresolved — NOT fabricated; flagged for a resolve-dvids-r03 run"
metrics:
  tasks_completed: "4 of 5 (Task 5 = blocking human-verify checkpoint)"
  rows: "222 -> 294"
  completed_date: "2026-06-15"
---

# Phase quick-260615-3e3 Plan 01: Fetch War.gov Release 03 Summary

Re-fetched war.gov's third UAP tranche ("Release 03", 6/12/26, 72 rows) verbatim,
wired its slideshow + bundle assets into the downloader, normalised the rows into
the wargov archive (222 → 294 records), extended the release-batch filter + landing
page to recognise Release 03, and rebuilt dist/ with Pagefind indexing the new
records. **Stopped at Task 5** — a blocking human-verify checkpoint requiring local
preview + the GitHub-Release / R2 binary upload (NOT performed autonomously).

## What Was Done

### Task 1 — Discovery gate PASSED (commit `b05e164`)
- Baseline fingerprint: `[('5/22/26', 64), ('5/8/26', 158)]` (222 rows).
- Re-downloaded `uap-data.csv` via `scripts/download-war.gov.py` (curl_cffi Chrome
  impersonation). New file 366 KB vs prev 298 KB.
- New fingerprint: `[('5/22/26', 64), ('5/8/26', 158), ('6/12/26', 72)]`.
- **Release 03 = date `6/12/26`, 72 rows: 53 PDF, 10 IMG, 6 VID, 3 AUD.**
- **CSV header drift detected:** a new leading column `Featured` (10 rows = `YES`)
  appears before `Redaction`. Flagged for Task 3.

### Task 2 — Downloader entries (commit `3b9b751`)
- Discovered the REAL Release-03 asset URLs from the live `war.gov/UFO/` page markup
  (did NOT hardcode guesses — the first guessed bundle URL 404'd):
  - Slideshow/Rotator: `…/061226/Rotator/` — 10 images (the `Featured=YES` hero picks).
  - Document bundle: `…/061226/release_03/release_03_documents.zip` (866 MB, HEAD 200).
  - Video archive: `cloudfront…/release_03/uap_videos_061226.zip` (4.96 GB, HEAD 200).
- Added `SLIDESHOW_3_BASE` + `SLIDES_3` (10) + `SLIDES_3_DIR = slideshow-3/`, wired
  the fetch into `main()`, appended both bundle tuples to `BUNDLES[]`.
- Fetched all 10 slideshow-3 images (tracked in git; bundles stay gitignored).

### Task 3 — Normalise + release-batch parity (commit `50342f2`, TDD)
- RED→GREEN: added `6/12/26 → '03'` to BOTH `Card.astro` `releaseBatch()` and
  `normalize-csv.py` `render_card_html` (D-10 byte-parallel contract).
- Documented the new `Featured` column as a lenient `z.string().default('')` in
  `content.config.ts` (D-02 drift signal; wargov schema is non-strict so no hard-fail).
- Regenerated `data/wargov.json` + shards (+ public mirror): 222 → 294 rows, 5 → 6
  shards. `normalize-csv.py --check` exits 0. `uap-data.csv` unchanged (git diff clean).
- PDFs rewrite to `assets.realufo.org/pdfs/wargov/<basename>`; IMGs preserved verbatim.

### Task 4 — Landing-page copy + build (commit `1040313`)
- Release filter `<select>`: added `Release 03 (June 12, 2026)`.
- Hero carousel: +2 R03 slides (FBI Colorado Springs digital rendering; US Army 1949
  flying-saucer study).
- Headlines: new Release 03 head-card (substantive, no filler per §9); archive total
  `222` → `294`.
- `totalRecords` now computed EXACTLY from primary rows + shard card counts (was an
  upper-bound estimate) → footer reads 294.
- SEO description, JSON-LD name/description/keywords, classified stamp, carousel label:
  all extended to Release 03.
- `pnpm build` succeeded; `dist/index.html` contains `data-release="03"` cards (18 in
  index + 54 in shards = 72); `dist/pagefind/` regenerated and a decompressed fragment
  contains R03 text ("Orbs Over the Pond" / "US Army-Flying-Saucer-Study").

## Deviations from Plan

### Auto-fixed / discovery adjustments (no user permission needed)

**1. [Rule 3 - Blocking] Release-03 asset paths differ from the plan's assumptions**
- Plan assumed `Slideshow-3/` + `release_03_document_bundle.zip`. Live war.gov markup
  revealed `/061226/Rotator/` + `release_03_documents.zip` + cloudfront
  `release_03/uap_videos_061226.zip`. Used the discovered (HEAD-200-verified) URLs.
- Files: `scripts/download-war.gov.py`. Commit `3b9b751`.

**2. [Rule 2 - Correctness] Exact record total instead of upper-bound estimate**
- The pre-existing `totalRecords = rows.length + shards.length*50` over-counted (would
  show 350 for 294 actual). Replaced with an exact build-time shard-card count so the
  footer matches the head-card "294". Files: `src/pages/index.astro`. Commit `1040313`.

**3. CSV column drift (`Featured`)** — documented in `content.config.ts` per Task 1
step 4 / Task 3 step 2. Not a workaround — an intentional, documented schema addition.

## Known Stubs / Deferred Items

**9 Release-03 VID/AUD records have no DVIDS→DOD asset mapping** (DVIDS IDs
1010263, 1010264, 1010267, 1010269, 1010272, 1010276, 1010319, 1010336, 1010337).
These IDs are absent from `scripts/dvids2dod-r01.json` / `dvids2dod-r02.json`, so the
normaliser left their `PDF | Image Link` empty (NOT fabricated). Consequence: those 9
cards render WITHOUT an Open/Play button (the DVIDS ↗ external button still works).
**A `resolve-dvids-r03` run must populate a `dvids2dod-r03.json` before these play.**
This is flagged for the Task 5 human checkpoint.

## Threat Flags

None new. Release-03 row text flows through the existing `_e()` (html.escape) +
`rewrite_to_r2` mitigations (T-3e3-02 / T-3e3-04 unchanged). `_assert_csv_unchanged`
held (T-3e3-01). curl_cffi pre-existing dependency, no new installs (T-3e3-SC).

## Self-Check: PASSED

- Commits exist: `b05e164`, `3b9b751`, `50342f2`, `1040313` (all in git log).
- `slideshow-3/` (10 images) tracked; `data/wargov-shard-6.json` created.
- `data/wargov.json` = 294 rows; `--check` clean; `uap-data.csv` git-clean.
- `dist/index.html` has `data-release="03"` + "294"; Pagefind indexes R03.

## Checkpoint Status

**Task 5 (checkpoint:human-verify, gate="blocking-human") NOT executed.** The release
upload + R2 mirror were intentionally left to the human per plan constraints. See the
"CHECKPOINT REACHED" report returned to the orchestrator for the exact verification
steps + upload commands.

---

## Task 5 — RESUMED & COMPLETED (2026-06-15)

Human supplied the binaries (`/Users/laichan/UFO/release_03_documents` = 53 PDFs / 828 MB; `/Users/laichan/UFO/AARO061226` = 9 mp4 / 4.7 GB). Resumed via `/gsd:quick resume`.

**DVIDS gap closed:** Created `scripts/resolve-dvids-r03.py` (modelled on r01), scraped dvidshub.net for the 9 IDs → `scripts/dvids2dod-r03.json`. Wired into `normalize-csv.py` `DVIDS_MAP_PATHS`. Re-normalised → VID hydration 95 mp4 URLs (+9), all 9 R03 VID/AUD cards now render Play buttons. `pnpm build` clean, Pagefind 57 pages. Commit `4dc5255`.

**R2 upload (bucket `realufo`, prod CDN `assets.realufo.org`):**
- 53 PDFs → `pdfs/wargov/` — all HTTP 200 ✓
- 7 of 9 videos → `videos/wargov/DOD_<id>.mp4` — HTTP 200 ✓
- 2 large videos FAILED — wrangler `r2 object put` caps at 300 MiB: `DOD_111764796.mp4` (2.99 GiB), `DOD_111764902.mp4` (1.19 GiB). Need S3 multipart (aws/rclone + R2 S3 keys). Until uploaded, DVIDS 1010319 + 1010336 (both AUD) play → 404. Tracked in STATE.md TODOs.

**Remaining (operator):** upload the 2 large videos via S3 path; optional GH Releases cold-storage backup (`pdfs-v1`/`videos-v1`, D-06 — cards do NOT reference these); merge branch `quick/260615-3e3-wargov-release-03`.
